function enviarCadastro(event) {
    event.preventDefault();
    // Adicione aqui o código para processar o formulário, por exemplo, enviar para o servidor.
    alert("Cadastro enviado! (Adicione o código de processamento)");
  }